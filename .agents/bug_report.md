1. the dashboard click-to-use feature 만들기
    설명: wiki prompt list|show|trace|eval and wiki insight ... are developer/debug CLIs, not end-user surfaces. The click-driven surface is the plugin dashboard. If you want insights/promotion/trace to be "딸깍딸깍" in the dashboard, that's a small plugin feature goal (wire the existing incuratorClient methods to dashboard buttons) — separate from the search question; I can spec it whenever you want.    

2. PDF 481페이지 하단에서 언급된 "A decomposition of $Q^*_{\infty}$ as $H \tilde{I} H^T$ may be easily computed from its eigenvalue decomposition (see section A4.2(p580) for Jacobi’s algorithm for this)" 구절을 긁어다가 sidebar chat에 넣고 이 부분을 설명해달라고 했는데, 현재 페이지(PDF)에 대해 설명해줌. 이 버그 해결좀

3. Obsidian agent에서 settings창에서 AI Provider, model 바꿨는데 sidebat chat에서는 안바뀜. 둘이 동기화되어야함.