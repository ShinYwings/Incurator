# Failure Atlas case record — see docs/specs/failure_atlas/FAILURE_ATLAS.md §2
id: F9
title: Authored note topology (wikilinks/embeds/tags/frontmatter refs) is never compiled into the graph
query_family: compiler
execution_mode: deterministic
status: assigned
status_history:
  - date: "2026-06-11"
    status: suspected
    evidence: "Umbrella plan §4, Arena consensus"
  - date: "2026-06-12"
    status: reproduced
    evidence: "backend/tests/test_failure_atlas_repro.py::test_f9_baseline_authored_wikilinks_not_compiled"
  - date: "2026-06-12"
    status: assigned
    evidence: "FAILURE_ATLAS.md §8 — note-native IR / authored topology is Program 2 P2.1 scope"
owner: program-2
impact: >
  A human-authored wikilink is the highest-confidence semantic edge in the
  vault, yet the compiled graph contains only LLM-inferred relations. Authored
  and extracted topology are not merely indistinguishable — authored topology
  is absent entirely, so retrieval and graph traversal cannot use the
  strongest signal the vault offers (vault-as-codebase: imports are ignored).
boundary:
  module: backend/src/curator/pipeline
  symbol: compile_source_l2
  detail: >
    The L1→L4 compile path (source_spans/knowledge_units/graph_index) never
    converts an authored [[link]], embed, tag, or frontmatter ref into a graph
    edge. The baseline fixture records this absence at L1; the v0.39 oracle is
    deliberately re-pinned to the real compile/publish boundary so a row-count
    helper cannot falsely claim closure.
fixture: backend/tests/test_failure_atlas_repro.py::test_f9_baseline_authored_wikilinks_not_compiled
commands:
  - scripts/backend-check pytest "backend/tests/test_failure_atlas_repro.py::test_f9_baseline_authored_wikilinks_not_compiled" -q
oracles:
  deterministic: >
    Compiling a registered visible Markdown note through the real production
    boundary produces deterministic active graph_relations with
    edge_class=authored, current generation ownership, exact canonical
    endpoints, and relation types for supported links/embeds/tags/frontmatter
    refs. They affect active topology but create no graph_relation_supports and
    never become factual report evidence. Unchanged rebuilds and cross-device
    compilation converge; edits, rename/delete, ambiguity, unsafe targets, and
    failed publication reconcile or fail closed without stale/partial topology.
  semantic: null
before_state_evidence: >
  At baseline, storing L1 spans for a markdown note containing [[Residual
  Learning]] and [[Euler Method]] wikilinks leaves graph_relations and
  dag_edges completely empty — no record anywhere encodes the authored links.
observed_result: >
  Reproduced 2026-06-12 — after L1 compile of a wikilinked note:
  graph_relations rows = 0, dag_edges rows = 0; authored links nowhere in DB.
snapshot:
  git_sha: 8458f652d5481f377bd957ed1256240aaf470f54
  version: 0.5.6
  schema_version: 6
  scenario: complex_math_backprop
assignment:
  program: program-2
  gate: >
    v0.39 production-boundary tests prove deterministic authored wikilinks,
    embeds, derived backlinks, exact alias resolution, tags, and frontmatter
    refs compile as lifecycle-aware topology distinct from extracted factual
    support, including atomic reconciliation and replica convergence.
notes: >
  Oracle test: test_f9_oracle_authored_links_compiled_as_topology (strict
  xfail until v0.39 implementation). The 2026-07-30 approved plan strengthens
  the original non-zero-row oracle because it did not call the production
  compiler; capture-before-repair evidence and the original baseline remain
  unchanged.
