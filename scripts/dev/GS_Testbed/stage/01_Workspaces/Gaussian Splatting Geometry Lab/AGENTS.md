# Workspace AI Agents Rules

This testbed workspace is a repo-local fixture for incurator validation.
Runtime-specific incurator rules are installed by `scripts/dev/GS_Testbed/create_testbed.py`
through the same workspace-rule templates used by `wiki workspace init` and
`wiki mcp connect`.
